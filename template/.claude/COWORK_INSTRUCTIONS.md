# Cowork instructions (generated stub)

This file is GENERATED by `/psotobverse-utils:sync-cowork` after your first Claude Code session.
The command concatenates the constitution with the project overlay and writes the
result below. Copy the block between the markers and paste it into the
**Instructions** field of the Cowork project. Cowork does not auto-read
`CLAUDE.md`, so this paste is what carries the rules into Cowork.

Until you run `/psotobverse-utils:sync-cowork`, this minimal block applies.

[PASTE FROM HERE]

Read-first order: constitution -> policies/00-index -> knowledge-map -> PROJECT_BRIEF.

- Do not invent. Anchor every factual claim to evidence (file:line, output, DOI).
  If you cannot anchor it, say "unknown" or "I don't know" — never fabricate data
  or citations.
- Mark uncertainty. Separate fact from inference from hypothesis from decision.
- Ask before acting. Propose irreversible or outward-facing actions (push, delete,
  publish, send) and wait for explicit confirmation.
- Write to outputs/. All generated artifacts go under `outputs/`.
- Cowork does not auto-read CLAUDE.md; these instructions are the source of rules here.

[END]
