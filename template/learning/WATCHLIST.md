# Analyst watchlist — open items (living, pruned)

A **living** list the agent maintains: things to verify, anomalies or red flags it
noticed, and TODOs for later so nothing is forgotten. **Prune on resolution** — do
not let it grow indefinitely; git history is the archive, this file is the *open*
set. Distinct from `LEARNING_LOG.md` (append-only notes) and `PLAYBOOK.md`
(durable lessons).

## To verify
- [ ] (example) Confirm the join key is unique in `raw` before the cohort join.

## Anomalies / red flags
- [ ] (example) Outlier cluster in <variable> — inspect before deciding handling.

## Later / don't forget
- [ ] (example) Add a sensitivity analysis excluding <subgroup>.

<!-- Remove an item when it is resolved. Keep this list short and current. -->
