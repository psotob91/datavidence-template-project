# Session handoffs — index (newest at bottom)

Each line points to a dated handoff written by the `SessionEnd` hook. Open the
file only when you need that session's detail; this index is the cheap entry point.
