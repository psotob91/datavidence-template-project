# SESSION_STATE — current state of {{ project_name }}

> **Rolling file. OVERWRITE it, don't append.** Keep it to ~1 screen. The
> `SessionStart` hook injects its head so a new session resumes cheaply; durable
> history lives in `learning/sessions/` (handoffs) and `git log`. This file is
> only "where we are right now + what's next".

- **Updated:** (set on first real session)
- **Branch:** `main`
- **Status:** fresh project — fill in `PROJECT_BRIEF.md`, run `make setup`, then start work.

## Done this session
- (nothing yet — project just generated from the datavidence template)

## Next steps
1. Complete `PROJECT_BRIEF.md` (goal, population, outcome/exposure, data source, audience).
2. Install the companion plugin and run `make setup` (see `README.md`).
3. Begin the analysis flow (`analysis/01_ingest…`).

## Uncommitted
- (none — initial state)
