# Keep this directory under version control even when empty.
# R/ holds reusable functions for the {targets} pipeline (sourced from _targets.R).
# One responsibility per file; name files after what they do (e.g. clean-data.R).
