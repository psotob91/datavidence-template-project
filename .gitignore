# Template-repo hygiene (the child project's .gitignore lives in template/.gitignore)
**/__pycache__/
*.pyc
# copier render-test scratch
/.render-test/
/tmp-render*/
# factory meta-learner: machine-local dirty-bit + transient signal buffer
# (durable handoffs in learning/sessions/ and the learning/*.md files ARE committed)
/.claude/state/
/learning/queue/
# raw source material for health-data policies (large .mhtml Gemini exports + prior
# integration drafts). Distilled into docs/research/health-data-policies-research.md and
# template/docs/health/; kept local only — never commit the binaries.
/temporal-expansion-ideas/
# derived index — regenerate with: python scripts/reindex.py (recipe is committed)
/llms.txt
